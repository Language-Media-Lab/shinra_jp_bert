group=JP-5
question_type=attribute,question,example

# example_num=1
example_num=1
#seed=41
#dir_name=2019JP_attr_q_seed${seed}_example${example_num}
#bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}
seed=42
dir_name=2019JP_attr_q_seed${seed}_example${example_num}
bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}
#seed=43
#dir_name=2019JP_attr_q_seed${seed}_example${example_num}
#bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}
#seed=44
#dir_name=2019JP_attr_q_seed${seed}_example${example_num}
#bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}
#seed=45
#dir_name=2019JP_attr_q_seed${seed}_example${example_num}
#bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}

# example_num=5
example_num=5
seed=41
dir_name=2019JP_attr_q_seed${seed}_example${example_num}
bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}
seed=42
dir_name=2019JP_attr_q_seed${seed}_example${example_num}
bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}
seed=43
dir_name=2019JP_attr_q_seed${seed}_example${example_num}
bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}
seed=44
dir_name=2019JP_attr_q_seed${seed}_example${example_num}
bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}
seed=45
dir_name=2019JP_attr_q_seed${seed}_example${example_num}
bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}

# example_num=10
example_num=10
#seed=41
#dir_name=2019JP_attr_q_seed${seed}_example${example_num}
#bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}
seed=42
dir_name=2019JP_attr_q_seed${seed}_example${example_num}
bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}
#seed=43
#dir_name=2019JP_attr_q_seed${seed}_example${example_num}
#bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}
#seed=44
#dir_name=2019JP_attr_q_seed${seed}_example${example_num}
#bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}
#seed=45
#dir_name=2019JP_attr_q_seed${seed}_example${example_num}
#bash convert_shinra_to_squad.sh ${dir_name} ${question_type} ${example_num} ${seed}
